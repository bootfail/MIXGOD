import { describe, it, expect } from 'vitest'

// Real tests added when DropZone component is implemented in Plan 03
describe('DropZone', () => {
  it('renders drop zone', () => {
    expect(true).toBe(true)
  })
})
