---
phase: 01-track-library-analysis-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/package.json
  - src/client/vite.config.ts
  - src/client/vitest.config.ts
  - src/client/tailwind.config.ts
  - src/client/src/types/track.ts
  - src/client/src/types/project.ts
  - src/client/src/types/analysis.ts
  - src/client/src/test/setup.ts
  - src/server/MixGod.Api/MixGod.Api.csproj
  - src/server/MixGod.Api/Program.cs
  - src/server/MixGod.Api/Models/Track.cs
  - src/server/MixGod.Api/Models/Project.cs
  - src/server/MixGod.Api/Models/AnalysisResult.cs
  - src/server/MixGod.Api.Tests/MixGod.Api.Tests.csproj
  - src/analysis/analyzer.py
  - src/analysis/genre_taxonomy.py
  - src/analysis/requirements.txt
  - src/analysis/tests/conftest.py
  - src/analysis/tests/test_bpm.py
  - src/analysis/tests/test_key.py
  - src/analysis/tests/test_energy.py
  - src/analysis/tests/test_genre.py
autonomous: true
requirements:
  - ANALYSIS-01
  - ANALYSIS-02
  - ANALYSIS-03
  - ANALYSIS-04

must_haves:
  truths:
    - "Essentia correctly detects BPM for hardstyle tracks (150 not 75) via octave correction"
    - "Key detection uses EDM-specific profile and returns key + scale (e.g., 'A minor')"
    - "Energy score maps to 1-10 scale combining danceability, loudness, dynamic complexity"
    - "Genre classification returns primary + secondary genre mapped to user's hierarchy (e.g., 'Hardstyle > Raw Hardstyle')"
    - "All three projects scaffold and their test suites run green"
  artifacts:
    - path: "src/analysis/analyzer.py"
      provides: "Full track analysis (BPM, key, energy, genre) with octave correction"
      min_lines: 80
    - path: "src/analysis/genre_taxonomy.py"
      provides: "Discogs -> user genre hierarchy mapping"
      min_lines: 40
    - path: "src/client/src/types/track.ts"
      provides: "Track, AnalysisResult, AnalysisStatus TypeScript interfaces"
      exports: ["Track", "AnalysisResult", "AnalysisStatus", "GenreInfo"]
    - path: "src/server/MixGod.Api/Models/Track.cs"
      provides: ".NET Track model matching TypeScript interface"
    - path: "src/analysis/tests/test_bpm.py"
      provides: "BPM detection + octave correction tests"
      min_lines: 30
  key_links:
    - from: "src/analysis/analyzer.py"
      to: "essentia.standard"
      via: "import and algorithm calls"
      pattern: "es\\.RhythmExtractor2013|es\\.KeyExtractor|es\\.Danceability"
    - from: "src/analysis/analyzer.py"
      to: "src/analysis/genre_taxonomy.py"
      via: "import for genre mapping"
      pattern: "from genre_taxonomy import"
    - from: "src/client/src/types/track.ts"
      to: "src/server/MixGod.Api/Models/Track.cs"
      via: "shared contract (same shape)"
      pattern: "bpm.*key.*energy.*genre"
---

<objective>
Scaffold the entire MIXGOD project (React frontend, .NET API backend, Python analysis sidecar) and build the complete audio analysis pipeline with Essentia.

Purpose: Establish the foundation for all subsequent plans and deliver the core audio intelligence -- accurate BPM detection with octave correction for hardstyle/hardcore, key detection with EDM profiles, energy scoring, and subgenre-aware genre classification. This is the technical heart of Phase 1.

Output: Three runnable project scaffolds with test frameworks, shared type contracts, and a fully tested Python analysis script that can analyze any MP3/WAV/FLAC track.
</objective>

<execution_context>
@C:/Users/sjors/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sjors/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-track-library-analysis-pipeline/01-CONTEXT.md
@.planning/phases/01-track-library-analysis-pipeline/01-RESEARCH.md
@.planning/phases/01-track-library-analysis-pipeline/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold all three projects with dependencies, test frameworks, and shared type contracts</name>
  <files>
    src/client/package.json,
    src/client/vite.config.ts,
    src/client/vitest.config.ts,
    src/client/tailwind.config.ts,
    src/client/tsconfig.json,
    src/client/index.html,
    src/client/src/main.tsx,
    src/client/src/App.tsx,
    src/client/src/test/setup.ts,
    src/client/src/types/track.ts,
    src/client/src/types/project.ts,
    src/client/src/types/analysis.ts,
    src/server/MixGod.Api/MixGod.Api.csproj,
    src/server/MixGod.Api/Program.cs,
    src/server/MixGod.Api/Models/Track.cs,
    src/server/MixGod.Api/Models/Project.cs,
    src/server/MixGod.Api/Models/AnalysisResult.cs,
    src/server/MixGod.Api.Tests/MixGod.Api.Tests.csproj,
    src/analysis/requirements.txt,
    src/analysis/tests/__init__.py,
    src/analysis/tests/conftest.py
  </files>
  <action>
    **React Frontend (src/client/):**
    1. Create Vite React-TS project: `npm create vite@latest . -- --template react-ts` in src/client/
    2. Install production deps: `npm install react-resizable-panels @tanstack/react-table @tanstack/react-virtual wavesurfer.js @wavesurfer/react react-dropzone dexie zustand`
    3. Install Tailwind CSS v4: follow Vite integration guide (postcss plugin or @tailwindcss/vite)
    4. Install dev deps: `npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom @types/node`
    5. Configure vitest.config.ts: jsdom environment, setup file at src/test/setup.ts, path aliases
    6. Configure tailwind.config.ts: extend with neon-dark theme colors from RESEARCH.md theme tokens (bg.primary=#0a0a0f, bg.panel=#12121a, all neon colors, glow box-shadow utilities, font families Inter + JetBrains Mono)
    7. Create src/test/setup.ts: import @testing-library/jest-dom
    8. Create TypeScript interfaces in src/types/:
       - track.ts: Track interface with all fields (id, serverId, title, artist, bpm, bpmRaw, bpmCorrected flag, key, keyConfidence, energy, genrePrimary, genreSecondary, genreConfidence, duration, format, bitrate, sampleRate, dateAdded, filename, peaksUrl, analysisStatus: 'queued'|'analyzing'|'done'|'error', analysisConfidence, userOverrides). Also GenreInfo type with hierarchy.
       - project.ts: Project interface (id, name, createdAt, updatedAt), SmartPlaylist interface (id, projectId, name, filters: FilterPreset[]), FilterPreset type
       - analysis.ts: AnalysisResult interface (bpmRaw, bpmCorrected, bpmWasCorrected, key, keyScale, keyConfidence, energy, genrePrimary, genreSecondary, genreConfidence, danceability, loudness, duration, beatsConfidence)
    9. Minimal App.tsx with "MIXGOD" text to confirm render

    **ASP.NET Core Backend (src/server/MixGod.Api/):**
    1. Create project: `dotnet new webapi -n MixGod.Api` in src/server/
    2. Add packages: `dotnet add package NAudio` and `dotnet add package Microsoft.AspNetCore.SignalR` (SignalR optional, but include the package)
    3. Configure Program.cs: CORS for localhost:5173 (Vite dev), static file serving for audio-storage/, JSON serialization with camelCase
    4. Create Models/Track.cs: C# record matching the TypeScript Track interface (all same fields, using enums for AnalysisStatus)
    5. Create Models/Project.cs: C# record for project metadata
    6. Create Models/AnalysisResult.cs: C# record matching Python analyzer JSON output shape
    7. Create test project: `dotnet new xunit -n MixGod.Api.Tests` in src/server/, add reference to MixGod.Api, add NSubstitute package

    **Python Analysis Sidecar (src/analysis/):**
    1. Create requirements.txt: essentia, essentia-tensorflow, pytest
    2. Create tests/__init__.py (empty) and tests/conftest.py with placeholder fixtures
    3. Do NOT install Python deps yet (Essentia requires specific setup) -- just create the file structure

    **Root:**
    1. Create .gitignore entries for: audio-storage/, node_modules/, bin/, obj/, __pycache__/, .venv/, *.pyc
    2. Ensure audio-storage/ directory exists (gitignored)
  </action>
  <verify>
    <automated>cd src/client && npm run build && npx vitest run --reporter=verbose 2>&1 | head -20 && cd ../../src/server && dotnet build MixGod.Api && dotnet build MixGod.Api.Tests</automated>
  </verify>
  <done>
    - React app builds with zero errors, vitest runs (0 tests is OK for now)
    - .NET API builds, test project builds
    - All TypeScript types are defined and exported
    - All .NET models are defined
    - Python directory structure exists with requirements.txt
    - Tailwind configured with neon-dark theme tokens
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Python Essentia analysis pipeline with BPM octave correction, key, energy, and genre classification</name>
  <files>
    src/analysis/analyzer.py,
    src/analysis/genre_taxonomy.py,
    src/analysis/tests/conftest.py,
    src/analysis/tests/test_bpm.py,
    src/analysis/tests/test_key.py,
    src/analysis/tests/test_energy.py,
    src/analysis/tests/test_genre.py
  </files>
  <behavior>
    - BPM octave correction: raw 75 BPM with hardstyle genre hint -> corrected 150 BPM, was_corrected=True
    - BPM octave correction: raw 150 BPM -> no correction, was_corrected=False
    - BPM octave correction: raw 85 BPM no genre hint, doubled is 170 (in range) -> corrected 170
    - BPM octave correction: raw 130 BPM no genre hint -> no correction (already reasonable)
    - Genre taxonomy mapping: "Electronic---Hardstyle" -> {primary: "Hardstyle", secondary: null, hierarchy: ["Electronic", "Hardstyle"]}
    - Genre taxonomy mapping: unknown Essentia label -> {primary: "Unknown", secondary: null}
    - Genre taxonomy: user can define custom parent->child mappings
    - Energy normalization: combined score from danceability + loudness + dynamic complexity maps to 1-10 integer
    - Energy edge cases: minimum is 1, maximum is 10, never 0 or 11+
    - Full analysis: returns JSON dict with all required fields (bpm_raw, bpm_corrected, bpm_was_corrected, key, scale, key_confidence, energy, genre_primary, genre_secondary, genre_confidence, danceability, loudness, duration, beats_confidence)
  </behavior>
  <action>
    **IMPORTANT: First install Essentia.** Run `pip install essentia essentia-tensorflow` (or use a venv: `python -m venv src/analysis/.venv && src/analysis/.venv/Scripts/activate && pip install essentia essentia-tensorflow pytest`). If essentia-tensorflow fails on Windows, try `pip install essentia` alone first -- genre classification can use a fallback.

    **Write tests first (RED):**

    1. tests/conftest.py: Create fixtures for sample audio. Since we may not have real audio files in CI, create two test strategies:
       - Unit tests for BPM octave correction (pure function, no audio needed)
       - Unit tests for genre taxonomy mapping (pure function, no audio needed)
       - Unit tests for energy normalization (pure function, no audio needed)
       - Integration tests marked with @pytest.mark.integration that require real audio files (skipped if no fixtures exist)

    2. tests/test_bpm.py: Test correct_bpm_octave() function:
       - test_hardstyle_half_tempo_corrected: raw=75, genre_hint='hardstyle' -> (150, True)
       - test_already_correct_bpm: raw=150, genre_hint='hardstyle' -> (150, False)
       - test_frenchcore_half_tempo: raw=90, genre_hint='frenchcore' -> (180, True)
       - test_no_genre_hint_low_bpm: raw=85, genre_hint=None -> (170, True)
       - test_no_genre_hint_normal_bpm: raw=130, genre_hint=None -> (130, False)
       - test_house_bpm_correct: raw=128, genre_hint='house' -> (128, False)
       - test_uptempo_half: raw=110, genre_hint='uptempo' -> (220, True)

    3. tests/test_key.py: Test key result parsing:
       - test_key_result_format: result contains key string and confidence float
       - test_key_confidence_range: confidence is between 0.0 and 1.0

    4. tests/test_energy.py: Test normalize_energy() function:
       - test_energy_min_is_1: very low values -> 1
       - test_energy_max_is_10: very high values -> 10
       - test_energy_mid_range: moderate values -> 4-7
       - test_energy_returns_int: always returns integer

    5. tests/test_genre.py: Test GenreTaxonomy class:
       - test_map_discogs_hardstyle: "Electronic---Hardstyle" -> primary="Hardstyle"
       - test_map_discogs_hardcore: "Electronic---Hardcore" -> primary="Hardcore"
       - test_map_unknown_genre: "Unknown---Label" -> primary="Unknown"
       - test_hierarchy_depth: "Electronic---Hardstyle" -> hierarchy=["Electronic", "Hardstyle"]
       - test_custom_subgenre_mapping: can add custom mapping "Hardstyle" -> ["Raw Hardstyle", "Euphoric Hardstyle"]

    **Run tests -- all should FAIL (RED).**

    **Implement (GREEN):**

    6. genre_taxonomy.py: GenreTaxonomy class with:
       - DISCOGS_MAPPING dict: maps Essentia Discogs genre strings to user-friendly labels
       - GENRE_HIERARCHY dict: parent -> [children] relationships (Hardstyle > [Raw Hardstyle, Euphoric Hardstyle, Classic Hardstyle], Hardcore > [Frenchcore, Uptempo, Mainstream Hardcore], etc.)
       - map_genre(discogs_label: str) -> GenreResult dict (primary, secondary, confidence, hierarchy)
       - Support for user custom genres (add_custom_genre method)
       - Cover the genres from CONTEXT.md: house, hardstyle, hardcore, frenchcore, party (Dutch Feest), latin, urban

    7. analyzer.py: Main analysis script with:
       - correct_bpm_octave(raw_bpm, genre_hint) function per RESEARCH.md Pattern 2
       - normalize_energy(danceability, loudness, dynamic_complexity) function -> int 1-10
       - analyze_track(filepath) function that:
         a. Loads audio with es.MonoLoader(sampleRate=44100)
         b. BPM via es.RhythmExtractor2013(method="degara")
         c. Key via es.KeyExtractor(profileType='edma')
         d. Energy via es.Energy(), es.DynamicComplexity(), es.Danceability()
         e. Duration via es.Duration()
         f. Runs genre classification (if TensorFlow models available, else returns "Unknown")
         g. Applies BPM octave correction using genre result as hint
         h. Returns full JSON dict with all fields
       - CLI entry point: `python analyzer.py <filepath>` prints JSON to stdout
       - Graceful fallback: if essentia-tensorflow not installed, skip genre classification and return "Unknown"
       - Graceful fallback: if model files not found, log warning and return "Unknown" genre

    **Run tests -- all should PASS (GREEN).**
  </action>
  <verify>
    <automated>cd src/analysis && python -m pytest tests/ -x -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All BPM octave correction tests pass (hardstyle 75->150, frenchcore 90->180, normal BPM untouched)
    - All energy normalization tests pass (clamped 1-10, integer output)
    - All genre taxonomy tests pass (Discogs mapping, hierarchy, custom genres)
    - analyzer.py is callable as CLI: `python analyzer.py <file>` outputs valid JSON
    - Key detection tests pass (format and confidence range validated)
  </done>
</task>

</tasks>

<verification>
1. `cd src/client && npm run build` -- React app builds cleanly
2. `cd src/client && npx vitest run` -- test framework works (0 tests OK)
3. `cd src/server && dotnet build MixGod.Api && dotnet build MixGod.Api.Tests` -- .NET builds
4. `cd src/analysis && python -m pytest tests/ -x -v` -- all analysis tests pass
5. TypeScript types in src/client/src/types/ export Track, Project, AnalysisResult interfaces
6. .NET models in src/server/MixGod.Api/Models/ match TypeScript shapes
</verification>

<success_criteria>
- Three project scaffolds build without errors
- Python analysis pipeline correctly detects BPM with octave correction for hardstyle/hardcore
- Genre taxonomy maps Essentia Discogs labels to user-friendly hierarchy
- Energy normalization produces 1-10 integer scores
- All test suites run green
- Shared type contracts defined consistently across TypeScript and C#
</success_criteria>

<output>
After completion, create `.planning/phases/01-track-library-analysis-pipeline/01-01-SUMMARY.md`
</output>
